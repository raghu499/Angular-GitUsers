import { ColorHDirective } from './color-h.directive';

describe('ColorHDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorHDirective();
    expect(directive).toBeTruthy();
  });
});
